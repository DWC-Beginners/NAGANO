<h2>ログイン</h2>

<h3>会員の方はこちらからログイン</h3>

<div class="container">
  <div class="row">
    <div>
      <table>
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="field">
          <tr>
            <td>メールアドレス</td>
            <td> <%= f.email_field :email, autofocus: true, autocomplete: "sample@example.com" %></td>
          </tr>
          <tr>
            <td>パスワード</td>
            <td><%= f.password_field :password, autocomplete: "current-password" %></td>
          </tr>
          <tr>
            <td><%= f.submit "ログイン" %></td>
          </tr>
          <% end %>
        </div>
        <div>
          <h3>会員登録がお済みでない方</h3>
          <p>
            <%= link_to "こちら",  new_user_registration_path(registration) %>
            から新規登録を行ってください。</p>
        </div>
      </table>
    </div>
  </div>
</div>