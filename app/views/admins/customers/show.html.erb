

<div class="row">
  <div>
    <div>
      <h3><%= @customer.last_name + @customer.first_name %>さんの会員詳細</h3>
      <!--会員情報テーブル-->
      <table>
        <tr>
          <td>会員ID</td>
          <td><%= @customer.id %></td>
        </tr>
        <tr>
          <td>氏名</td>
          <td><%= @customer.last_name %><%= @customer.first_name %></td>
        </tr>
        <tr>
          <td>郵便番号</td>
          <td><%= @customer.postal_code %></td>
        </tr>
        <tr>
          <td>住所</td>
          <td><%= @customer.address %></td>
        </tr>
        <tr>
          <td>電話番号</td>
          <td><%= @customer.phone_number %></td>
        </tr>
        <tr>
          <td>メールアドレス</td>
          <td><%= @customer.email %></td>
        </tr>
        <!--会員のステータス(退会フラグ？)が有効か退会かで表示を変える-->
        <!--有効か退会かの判別が退会フラグだとわかりにくいかも？-->
        <!--trueだと退会、falseだと有効、デフォルトはfalse-->
        <tr>
          <td>会員ステータス</td>
          <td>
            <% if @customer.is_deleted %>
              退会
            <% elsif %>
              有効
            <% end %>
          </td>
        </tr>
        <tr>
          <td><%= link_to "編集する", edit_admin_customer_path(@customer), class:"btn btn-promary" %></td>
          <!--注文履歴一覧のリンク先はadminのorderのindexか？-->
          <td><%= link_to "注文履歴一覧を見る", admin_orders_path(@customer), class:"btn btn-promary" %></td>
        </tr>
      </table>
    </div>
  </div>
</div>