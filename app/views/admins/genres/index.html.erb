<div class="container">
  <div class="row">
    <h4 class="col offset-md-1 mb-md-5">ジャンル一覧・追加</h4>
  </div>
  <div class="row">
    <div class="col-md-2 offset-md-1 mb-md-5">
      <p>ジャンル名</p>
    </div>
    <div class="col">
      <%= form_with model:@genre,url: admins_genres_path, local:true do |f| %>
        <%= f.text_field :name, :placeholder => "ジャンル名" %>
        <%= f.submit '新規登録',class: "btn btn-success" %></td>
      <% end %>
    </div>
  </div>
  <div class="row">
    <div class="col-md-5 offset-md-1">
      <table class="table">
        <thead class="thead-light">
          <tr>
            <th>ジャンル</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
          <% @genres.each do |genre| %>
            <tr>
              <td><%= genre.name %></td>
              <td><%= link_to "編集する", edit_admins_genre_path(genre),class: "btn btn-success" %></td>
            </tr>
          <% end %>
        