<h1>ジャンル編集</h1>
<%= form_with model:@genre,url: admins_genre_path(@genre), local:true do |f| %>
  <tr>
    <td>ジャンル名</td>
    <td><%= f.text_field :name %></td>
    <td><%= f.submit '変更' %></td>
  </tr>
<% end %>
